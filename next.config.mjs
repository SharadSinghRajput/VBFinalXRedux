/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.aapkikismat.com', 'www.vinaybajrangi.com', 'images.unsplash.com'],
    },
    // trailingSlash: true,
};

export default nextConfig;
