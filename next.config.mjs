/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'www.aapkikismat.com',
            'www.vinaybajrangi.com',
            'images.unsplash.com',
            'vedicrishi.in'
        ],
    },
};

export default nextConfig;